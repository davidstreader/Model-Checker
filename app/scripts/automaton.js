/**
 * @public
 * @class
 * @param {string} name The name of the automaton
 * @param {Graph} graph A graph that represence the automaton
 */
function Automaton(name, graph) {
  this.name = name;
  this.graph = graph;
}

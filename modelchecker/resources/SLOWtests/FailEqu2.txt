processes {

One1 = a->One1.
One2 = a->STOP.
One4 = b->One4.
One5 = b->STOP.

Two1 = a->b->STOP.
Two2 = a->Two2|a->STOP.
Two3 = a->Two3|b->STOP.
Two4 = a->b->Two4.
Two5 = a->STOP|b->STOP.
Two8 = a->X|b->X, X = STOP.
//  Two9 = (a->STOP + b->STOP).

}


equation {
X|Y <f Y|X.
X|(Y|Z) <f (X|Y)|Z.

X|| Y <f Y||X.

X||X <f X.

//(X=>Y)|| (Z=>Y) * (X||Z)=>Y.

(a->STOP)=>X <f a->X.


(X|Y)=>Z <f (X=>Z) | (Y=>Z).

// X=>(Y+Z) ~ (X=>Y)|(X=>Z).

} 





/* Buffer Example */
processes {
    Buff = (in -> out -> Buff).
    B = a:Buff .
   B2a = (a:Buff/{x/a.out} || b:Buff/{x/b.in})\{x}.
   B3 = abs(B2a).
 /*    B4 = simp(B3).

    B2 = B4/ {in/a.in,out/b.out}.
    B2test = in->  X,
      X = (out->B2test|in->out->X).  */
}


operation {
//B2 ~ B2test.
}






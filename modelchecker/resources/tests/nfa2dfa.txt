processes {
     A = (a -> B | c-> D),
         B = (x->A|b->C),
         C = (a->B),
         D = (c->C|x->C)\{x}.

     B = nfa2dfa(A).
     C = abs(A).
 }
 automata A,B,C.
 
 operation {
     A # B.
 }

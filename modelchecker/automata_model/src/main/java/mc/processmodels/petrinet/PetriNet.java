package mc.processmodels.petrinet;


public class PetriNet /*extends ProcessModelObject implements ProcessModel */ {
/*    private PetriNetTransitions root;
    private Map<String, PetriNetTransitions> nodeMap;
    private Map<String, PetriNetEdge> edgeMap;
    private Map<String, List<PetriNetEdge>> alphabet;

    private int nodeId;
    private int edgeId;

    public PetriNet(String id) {
        super(id, "automata");
        setupPetriNet();

        // setup the root for this automaton
        this.root = addNode();
        root.setStartNode(true);
    }

    public void copyProperties(PetriNet fromThisPetriNet) {

    }

    public ProcessType getProcessType() {
        return ProcessType.PETRINET;
    }

    private void setupPetriNet(){
        this.nodeMap = new HashMap<>();
        this.edgeMap = new HashMap<>();
        this.alphabet = new HashMap<>();
        this.nodeId = 0;
        this.edgeId = 0;
    }*/
}
